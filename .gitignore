# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# IDE
.idea/
*.iml
.vscode/

# OS
.DS_Store

# User crypto profiles (may contain client keys). Example profiles under
# profiles/ are safe to commit; keep client-specific ones out with a .local.json
# suffix, e.g. acme-prod.local.json
*.crypticburp
profiles/*.local.json
